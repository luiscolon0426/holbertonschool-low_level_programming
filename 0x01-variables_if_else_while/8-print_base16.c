#include <stdio.h>
/**
 * main - numbers of base 16 in lowercase
 *
 * Return: 0
 */
int main(void)
{
	int;
	char digit;

	for (n = '0'; n <= '9'; n++)
		putchar(digit);

	for (ch = 'a'; ch <= 'f'; ch++)
		putchar(ch);

	putchar('\n');

	return (0);
}

