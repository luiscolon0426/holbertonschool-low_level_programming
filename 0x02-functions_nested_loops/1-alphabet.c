#include "main.h"
/**
 * main - print alpha in lowercase
 *
 * return: 0
 */
void print_alphabet(void);
{
	(char a);
	for (char = 'a'; char <= 'z'; char++)
		_putchar(char)
		{
			_putchar('\n')
		}
	return (0);
}
